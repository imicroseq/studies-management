# Server configuration
SERVER_PORT=3030

# OAuth2 credentials
EGO_URL="http://localhost:8081"
OAUTH_CLIENT_ID=
OAUTH_CLIENT_SECRET=
# Permission scope required to access the Studies service
SCOPES_WRITE="DOMAIN.WRITE"

# Song configuration,
SONG_1_PREFIX=
SONG_1_SAMPLE_TYPE=
SONG_1_URL=
# To add more SONG instances, increment the number suffix 
# for each variable (`SONG_2*`, `SONG_3*`, etc.)
# SONG_2_PREFIX="WW-"
# SONG_2_SAMPLE_TYPE=environmental
# SONG_2_URL="http://localhost:8090"
